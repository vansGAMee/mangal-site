"use client";
import { useEffect,useState } from "react";import Link from "next/link";
import { formatRubles } from "@mangal/design-system";
type Dashboard={admin:{email:string;role:string};orders:Array<{id:string;publicId:string;isTest:boolean;paymentStatus:string;fulfillmentStatus:string;totalKopecks:number;version:number;createdAt:string}>;warnings:Record<string,number|boolean>};
export function AdminDashboard(){const[data,setData]=useState<Dashboard|null>(null);useEffect(()=>{fetch("/api/admin/dashboard",{cache:"no-store"}).then(async response=>{if(response.status===401){location.assign("/admin/login");return}setData(await response.json() as Dashboard)}).catch(()=>setData(null))},[]);if(!data)return <p>Загрузка защищённых данных…</p>;return <><header style={{display:"flex",justifyContent:"space-between",alignItems:"center",padding:"24px 0",borderBottom:"1px solid #2b2a29"}}><div><b>Шаверма Воронеж · Admin</b><small style={{display:"block",color:"#999"}}>{data.admin.email} · {data.admin.role}</small></div></header><section style={{padding:"32px 0"}}><h1 style={{fontSize:48}}>Операционный dashboard</h1><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(210px,1fr))",gap:12}}>{Object.entries(data.warnings).filter(([,value])=>Boolean(value)).map(([key,value])=><div className="admin-card" key={key}><b>{warningLabel(key)}</b><p>{String(value)}</p></div>)}</div><h2 style={{marginTop:48}}>Последние заказы</h2><div style={{overflowX:"auto"}}><table style={{width:"100%",borderCollapse:"collapse"}}><thead><tr><th align="left">Заказ</th><th>Оплата</th><th>Исполнение</th><th align="right">Сумма</th></tr></thead><tbody>{data.orders.map(order=><tr key={order.id} style={{borderTop:"1px solid #2b2a29"}}><td style={{padding:"14px 0"}}><Link href={`/admin/orders/${order.id}`}>{order.isTest&&<span style={{background:"#f97316",color:"#000",padding:"1px 6px",borderRadius:3,fontWeight:"bold",marginRight:6,fontSize:11}}>ТЕСТ</span>}{order.publicId}</Link></td><td align="center">{order.paymentStatus}</td><td align="center">{order.fulfillmentStatus}</td><td align="right">{formatRubles(order.totalKopecks)}</td></tr>)}</tbody></table></div></section></>}
function warningLabel(key:string){return ({unpriced:"Есть товары без цены",noDeliveryZones:"Нет зон доставки",paymentRoutingIncomplete:"Эквайер не настроен",missingFiscal:"Фискальные поля не подтверждены",encryptionKeyMissing:"Нет активного encryption key",unknown:"Платежи UNKNOWN",reconciliation:"Очередь reconciliation",stuckRefunds:"Зависшие возвраты",businessIdentityMissing:"Нет реквизитов оператора"} as Record<string,string>)[key]??key}
