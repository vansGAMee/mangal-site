"use client";
import { useState } from "react";
export function AdminLogin() { const [error,setError]=useState(""); const [busy,setBusy]=useState(false); async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setError("");const form=new FormData(event.currentTarget);const response=await fetch("/api/admin/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:form.get("email"),password:form.get("password"),mfaCode:form.get("mfaCode")||undefined})});if(response.ok)location.assign("/admin");else{const body=await response.json() as {error:string};setError(body.error==="mfa_required"?"Введите код MFA или recovery code.":"Вход не выполнен.");setBusy(false);}} return <form onSubmit={submit} className="admin-card mx-auto mt-24 grid max-w-md gap-5"><div><p style={{color:"#c89d5c",fontSize:12}}>ЗАЩИЩЁННЫЙ КОНТУР</p><h1 style={{fontSize:42,margin:"8px 0"}}>Шаверма Воронеж · Admin</h1></div><label>Email<input className="admin-field" name="email" type="email" autoComplete="username" required /></label><label>Пароль<input className="admin-field" name="password" type="password" autoComplete="current-password" required /></label><label>TOTP / recovery code<input className="admin-field" name="mfaCode" inputMode="numeric" autoComplete="one-time-code" /></label>{error?<p role="alert">{error}</p>:null}<button className="admin-button" disabled={busy}>{busy?"Проверка…":"Войти"}</button></form>; }
