import { z } from "zod";
import { validateAdminMutation } from "@/server/admin/auth";
import { db } from "@/server/shared/db";
import { requestId } from "@/server/security/http";
const Schema=z.object({version:z.number().int().positive(),name:z.string().trim().min(1).max(160).optional(),compositionText:z.string().trim().max(1000).nullable().optional(),portionNote:z.string().trim().max(240).nullable().optional(),basePriceKopecks:z.number().int().nonnegative().nullable().optional(),unitPriceKopecks:z.number().int().nonnegative().nullable().optional(),displayPriceLabel:z.string().trim().min(1).max(80).optional(),requiresPriceConfirmation:z.boolean().optional(),isOrderable:z.boolean().optional(),isAvailable:z.boolean().optional(),fiscalVatCode:z.string().trim().min(1).max(40).nullable().optional(),fiscalPaymentSubject:z.string().trim().min(1).max(80).nullable().optional(),fiscalPaymentMode:z.string().trim().min(1).max(80).nullable().optional(),fiscalMeasure:z.string().trim().min(1).max(80).nullable().optional()}).strict();
export async function PATCH(request:Request,context:{params:Promise<{productId:string}>}):Promise<Response>{const id=requestId(request);try{const admin=await validateAdminMutation(request);const parsed=Schema.safeParse(await request.json());if(!parsed.success)return Response.json({error:"validation"},{status:400});const{productId}=await context.params;const current=await db.product.findUnique({where:{id:productId}});if(!current)return new Response(null,{status:404});if(current.version!==parsed.data.version)return Response.json({error:"version_conflict"},{status:409});const next={...current,...parsed.data};if(next.requiresPriceConfirmation&&(next.isOrderable||next.basePriceKopecks!==null||next.unitPriceKopecks!==null))return Response.json({error:"unconfirmed_price_must_be_blocked"},{status:409});if(next.pricingType==="FIXED"&&!next.requiresPriceConfirmation&&next.basePriceKopecks===null)return Response.json({error:"fixed_price_required"},{status:409});if(next.pricingType==="PER_KILOGRAM"&&next.unitPriceKopecks===null)return Response.json({error:"kilogram_price_required"},{status:409});const changes={...(parsed.data.name!==undefined?{name:parsed.data.name}:{}),...(parsed.data.compositionText!==undefined?{compositionText:parsed.data.compositionText}:{}),...(parsed.data.portionNote!==undefined?{portionNote:parsed.data.portionNote}:{}),...(parsed.data.basePriceKopecks!==undefined?{basePriceKopecks:parsed.data.basePriceKopecks}:{}),...(parsed.data.unitPriceKopecks!==undefined?{unitPriceKopecks:parsed.data.unitPriceKopecks}:{}),...(parsed.data.displayPriceLabel!==undefined?{displayPriceLabel:parsed.data.displayPriceLabel}:{}),...(parsed.data.requiresPriceConfirmation!==undefined?{requiresPriceConfirmation:parsed.data.requiresPriceConfirmation}:{}),...(parsed.data.isOrderable!==undefined?{isOrderable:parsed.data.isOrderable}:{}),...(parsed.data.isAvailable!==undefined?{isAvailable:parsed.data.isAvailable}:{}),...(parsed.data.fiscalVatCode!==undefined?{fiscalVatCode:parsed.data.fiscalVatCode}:{}),...(parsed.data.fiscalPaymentSubject!==undefined?{fiscalPaymentSubject:parsed.data.fiscalPaymentSubject}:{}),...(parsed.data.fiscalPaymentMode!==undefined?{fiscalPaymentMode:parsed.data.fiscalPaymentMode}:{}),...(parsed.data.fiscalMeasure!==undefined?{fiscalMeasure:parsed.data.fiscalMeasure}:{})};const updated=await db.$transaction(async tx=>{const result=await tx.product.updateMany({where:{id:productId,version:parsed.data.version},data:{...changes,version:{increment:1}}});if(!result.count)return null;await tx.adminAuditLog.create({data:{adminUserId:admin.id,action:"PRODUCT_UPDATED",targetType:"Product",targetId:productId,metadata:{changedFields:Object.keys(changes)},requestId:id}});return tx.product.findUnique({where:{id:productId}})});if(!updated)return Response.json({error:"version_conflict"},{status:409});return Response.json(updated)}catch{return new Response(null,{status:403})}}
export async function DELETE(request:Request,context:{params:Promise<{productId:string}>}):Promise<Response>{const id=requestId(request);try{const admin=await validateAdminMutation(request);const{productId}=await context.params;await db.product.delete({where:{id:productId}});await db.adminAuditLog.create({data:{adminUserId:admin.id,action:"PRODUCT_DELETED",targetType:"Product",targetId:productId,requestId:id,metadata:{}}});return new Response(null,{status:204})}catch{return new Response(null,{status:403})}}
