# ADR-004: Configurable PII legal basis

Status: requires legal approval before production.

`StoreSettings.legalBasis` is CONTRACT or CONSENT. Under CONSENT the unchecked personal-data checkbox is mandatory. Under CONTRACT ordering is not blocked by the checkbox and the privacy notice is recorded as acknowledged. Marketing is always an independent optional consent and declined decisions are preserved. Counsel must map each processing operation to a lawful basis.
